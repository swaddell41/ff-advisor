# Python dependencies for the Vercel function (api/index.py).
# Local dev installs from backend/pyproject.toml instead.
fastapi>=0.115
requests>=2.32
python-dotenv>=1.0
anthropic>=0.40
psycopg2-binary>=2.9
